spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_URL}:3306/assignment
    password: ${DB_PASS}
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
